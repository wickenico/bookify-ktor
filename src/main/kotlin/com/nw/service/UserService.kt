package com.nw.service

/*
class UserService(private val userFacade: UserFacade) {
    suspend fun getAllUsers() = userFacade.allUsers()
    suspend fun getUserById(id: Int) = userFacade.user(id)
    suspend fun getUserByIdandPassword(id: Int, password: String) = userFacade.findUserByIdAndPassword(id, password)
    suspend fun createNewUser(user: User) = userFacade.addNewUser(user)
    suspend fun updateUser(user: User) = userFacade.editUser(user)
    suspend fun deleteUserById(id: Int) = userFacade.deleteUser(id)
}*/
